name: Secret Environment Variable

on:
  workflow_dispatch:

jobs:
  env-secret:
    runs-on: ubuntu-latest

    steps:
      - name: Use Secret as Environment Variable
        env:
          MESSAGE: ${{ secrets.MY_SECRET_MESSAGE }}

        run: |
          echo "Secret is available."
          echo "Secret length: ${#MESSAGE}
