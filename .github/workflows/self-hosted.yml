name: Self Hosted Runner Test

on:
  workflow_dispatch:

jobs:
  test:
    runs-on: self-hosted

    steps:
      - name: Show machine info
        run: |
          hostname
          pwd
          du -f

      - name: Create file
        run: |
          echo "Runner is working" > test.txt
          ls
